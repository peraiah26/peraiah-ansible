- name: install and start the packages
  hosts: frontend
  become: yes
  tasks:
  - name: install zip
    ansible.builtin.package:
      name: zip
      state: present

  - name: start zip
    ansible.buitin.service:
      name: zip
      state: started
      enabled: yes
